Update Python interface

OpenBLAS is required (C version) for dsbmv (MAYBE WRITE OWN INTERFACE, remove
unnessecary headers then of course and be careful to update README's, also
update Makefile to linke not against stuff I do not need, somehow, I can compile
the library without the header cblas.h?, seems I also do not need to link
against lcblas when creating the lakcalc library file.)

Compute condition of matrix to check for numerical stability. Print condition
just before the eigenproblem is solved. Expalin in README and theory.

Update main READMEs
    - Stop marking variable and file names with '...' in main READMEs
    - Stop using "we" in main READMEs
    - j behaviour was unified with library functions)

Fix References in READMEs + code and align them with theory

