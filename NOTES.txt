Compute condition of matrix to check for numerical stability. Print condition
just before the eigenproblem is solved. Expalin in README and theory.

Update main READMEs
    - Stop marking variable and file names with '...' in main READMEs
    - Stop using "we" in main READMEs
    - j behaviour was unified with library functions)

Fix References in READMEs + code and align them with theory
